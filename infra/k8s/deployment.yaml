apiVersion: apps/v1
kind: Deployment
metadata:
  name: ragx-api
  namespace: ragx
  labels:
    app: ragx-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ragx-api
  template:
    metadata:
      labels:
        app: ragx-api
    spec:
      containers:
        - name: api
          image: <ECR_URL>/ragx:IMAGE_TAG
          ports:
            - containerPort: 8000
          env:
            - name: APP_ENV
              value: production
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ragx-secrets
                  key: groq-api-key
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: ragx-api
  namespace: ragx
spec:
  selector:
    app: ragx-api
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ragx-frontend
  namespace: ragx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ragx-frontend
  template:
    metadata:
      labels:
        app: ragx-frontend
    spec:
      containers:
        - name: frontend
          image: <ECR_URL>/ragx-frontend:IMAGE_TAG
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ragx-frontend
  namespace: ragx
spec:
  selector:
    app: ragx-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
